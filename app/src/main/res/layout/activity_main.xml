<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.glowingegg.qonzor.flykur.MainActivity"
    android:background="@color/coolGreen">

    <com.github.florent37.materialtextfield.MaterialTextField
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:mtf_image="@android:drawable/ic_menu_search"
        android:layout_marginTop="0dp"
        android:id="@+id/materialSearch">
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:ems="10"
            android:id="@+id/searchTextView"
            android:hint="@string/search_hint"
            android:layout_weight="100"
            android:background="@android:color/darker_gray" />
    </com.github.florent37.materialtextfield.MaterialTextField>

    <GridView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/pictureGrid"
        android:numColumns="2"
        android:columnWidth="190dp"
        android:stretchMode="columnWidth"
        android:verticalSpacing="10dp"
        android:horizontalSpacing="10dp"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:layout_centerInParent = "true"
        android:minHeight="250dp"
        android:layout_below="@+id/materialSearch">

    </GridView>

</RelativeLayout>
